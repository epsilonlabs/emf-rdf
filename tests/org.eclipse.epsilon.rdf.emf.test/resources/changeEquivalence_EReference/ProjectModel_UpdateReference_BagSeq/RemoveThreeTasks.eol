import "../TestOperations.eol";

	var p = Project.all.first();
	p.println();
	
	if(consoleOutput) {
		"\n-Start-\n".println();
		
	}
	showProjectTask(p);
	
	p.tasks.remove(p.tasks.first());
	p.tasks.remove(p.tasks.first());
	p.tasks.remove(p.tasks.first());
	
	showProjectTask(p);
	
	if(consoleOutput) {
		showProjectTask(p);
		"\n-End-\n".println();
	}




